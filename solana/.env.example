# Solana dashboard + Python execute path (Jupiter /swap + Jito bundles).
# Anchor programs in programs/arb + programs/liq are stubs — do not send to
# Arb1111… / Liq1111…. Live arb hits Jupiter v6; live liq hits Solend.

# SOLANA_RPC=https://api.mainnet-beta.solana.com   # GPA-capable preferred; public RPC is fallback
SOL_FUNDER=4BHGQ9CXhajxDq5b3jvKfimsXEsFHoHUi2qg21qYVnGy
# SOL_SPONSOR=   # auto-filled from solana/keys/sponsor.json (pubkey only here)
# SOL_BOT=       # auto-filled from solana/keys/bot.json
# SOL_KEYPAIR=./solana/keys/bot.json
# SOL_SIM_ONLY=1          # default ON — LIVE only after Broadcast → Arm
# MIN_SOL_ARB_USD=0.05
# MIN_SOL_LIQ_USD=0.50
# Floor Jito tip (SOL). Actual LIVE tip = share of expected net, never enough to zero it.
# SOL_JITO_TIP_SOL=0.00001
# SOL_JITO_TIP_SHARE=0.15
# Optional Jito block-engine UUID (rate-limit auth)
# JITO_UUID=
# JITO_BLOCK_ENGINE=https://mainnet.block-engine.jito.wtf/api/v1/bundles
# Suggested fund amounts from funder:
#   sponsor 0.08 SOL (tips) · bot 0.25 SOL (CU + inventory)
# SOL_LIQ_PROGRAM=   # ignored if stub; Python talks to Solend directly
# SOL_ARB_PROGRAM=   # ignored if stub; Python talks to Jupiter /swap
